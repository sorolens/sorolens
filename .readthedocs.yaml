version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

mkdocs:
  configuration: packages/python-sdk/mkdocs.yml

python:
  install:
    - method: pip
      path: packages/python-sdk
      extra_requirements:
        - docs
